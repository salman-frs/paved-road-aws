global:
  domain: argocd.salmanfrs.dev

configs:
  cm:
    url: https://argocd.salmanfrs.dev
  params:
    server.insecure: true

server:
  ingress:
    enabled: true
    ingressClassName: nginx
    hostname: argocd.salmanfrs.dev

controller:
  metrics:
    enabled: true

repoServer:
  metrics:
    enabled: true
