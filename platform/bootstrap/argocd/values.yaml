configs:
  params:
    server.insecure: true

server:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - argocd.salmanfrs.dev

controller:
  metrics:
    enabled: true

repoServer:
  metrics:
    enabled: true
